
//import { getAdminInfo } from '@/api/getData'

export default {

    Login({ commit }){
       /*api.login(
		{
			account:this.loginForm.account,
			password:this.loginForm.password
		})*/
		console.log(commit)
	}
}